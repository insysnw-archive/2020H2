 [options]

Options:
      --address  ADDRESS        domain, ip address (tcp) or file path
                                (unix) to bind
      --port PORT               tcp port to bind (tcp only)
      --max-paket-size SIZE     limit paket size from client
      --max-client-count COUNT  limit simultaneous connections count
      --timeout MILLIS          timeout in milliseconds for any actions
                                from client
  -s, --single-login            only one client with the same name can
  -S, --sync                    use threads and blocking code
                                login
      --verb LEVEL              logging level: (none, fatal, error,
                                warning, info, verbose, debug)
  -d                            use debug log level
  -t, --tcp                     use tcp socket (default)
  -u, --unix                    use unix socket
  -v, --version                 show program version
  -h, --help                    print this help message
